<%# Barre de navigation %>
<nav class="nav navbar navbar-expand-lg bg-dark" style="background-color: #e3f2fd">
    <div class="container container-fluid">
        <%# Logo airbnb avec lien vers la page d'accueil %>
        <%= link_to root_path do %> 
            <%= image_tag "logo-airbnb.png", height: 40, width: 40 %>
        <% end %>

        <div class="navbar-collapse collapse" id="navbar">
            <ul class="navbar-nav ml-auto">

                <li class="align-center">
                    <%= link_to new_room_path do %>
                        <i class="fa fa-home fa-lg"></i> Devenez Hôte
                    <% end %>
                </li>

                <% if (!user_signed_in?) %>
                    <li class="nav-item">
                        <%= link_to t('Inscription'), new_user_registration_path, class: "nav-link" %></li>
                    <li class="nav-item">
                        <%= link_to t('Connexion'), new_user_session_path, class: "nav-link" %></li>
                    <% else %>
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <% if current_user.avatar.attached? %>
                                <%= image_tag current_user.avatar, height: 40, width: 40, class: "rounded-circle" %>
                                <%= current_user.fullname%>
                            <% else %>
                                <%= image_tag ("default_profil.jpg"), height: 40, width: 40, class: "rounded-circle" %>
                            <% end %>
                        </button>
                        <ul class="dropdown-menu ">
                            <li><a class="dropdown-item" href="#">Mes voyages</a></li>
                            <li><%= link_to 'Modifier mon profil', edit_user_registration_path %></li>
                            <li><%= link_to t('Deconnexion'), destroy_user_session_path, data: { "turbo-method": :delete } %></li>
                        </ul>
                    </div>
                <% end %>
            </ul>
        </div>
    </div>
</nav>